# Managed by chezmoi: ~/.local/share/root-staging → /etc/modprobe.d/blacklist-btusb.conf
#
# Keep the Bluetooth radio off at boot. The bluetooth.service drop-in
# (etc/systemd/system/bluetooth.service.d/auto-modprobe.conf) loads btusb on
# demand and unloads it on stop.
blacklist btusb
